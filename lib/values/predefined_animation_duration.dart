class PredefinedAnimationDuration {
  static const Duration instant = Duration.zero;
  static const Duration fastXXX = Duration(milliseconds: 50);
  static const Duration fastXX = Duration(milliseconds: 100);
  static const Duration fastX = Duration(milliseconds: 150);
  static const Duration fast = Duration(milliseconds: 200);
  static const Duration regular = Duration(milliseconds: 300);
  static const Duration slow = Duration(milliseconds: 500);
  static const Duration slowX = Duration(milliseconds: 750);
  static const Duration slowXX = Duration(milliseconds: 1000);
}
